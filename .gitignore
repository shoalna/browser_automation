.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.png
*.json
# Local scratch tests (smoke.py, videos, caches) — keep out of git
real_test/

# Local secrets — never commit
.env

# The agent XPath cache is a shareable artifact (URLs + XPaths, no secrets);
# allow committing it so teammates / CI can run without an API key.
!.browser_automation_agent_cache.json
